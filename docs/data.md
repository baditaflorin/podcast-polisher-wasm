# Data

Deployment Mode A has no offline data-generation pipeline and no static datasets.

Static metadata contract:

- `docs/version.json`
- `docs/manifest.webmanifest`
- `docs/404.html`

`make data` intentionally prints that Mode A has no generated data artifacts.
